{
  "name": "setup-patchver",
  "version": "0.1.0",
  "private": true,
  "description": "Setup PatchVer and add it to the PATH",
  "main": "index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt"
  },
  "author": "Diamond Drake Ventures",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/io": "^1.1.3",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
